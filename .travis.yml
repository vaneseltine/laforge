dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
  - "3.7-dev"
  - "3.8-dev"
  - "nightly"
install:
  - pip install -e .
  - pip install -r dev-requirements.txt
script:
  - flake8
  - pylint
